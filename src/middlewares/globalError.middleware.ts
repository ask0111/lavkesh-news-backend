// import { NextFunction, Request, Response } from 'express';
// import { errorResponse, StatusCode } from '../utils/responseHandler';

// export const errorHandler = (err: any, req: Request, res: Response, next: NextFunction) => {
//   console.error('Error:', err); // Log the error for debugging purposes
//   return errorResponse(res, 'An unexpected error occurred', err.message, StatusCode.INTERNAL_SERVER_ERROR);
// };
// //